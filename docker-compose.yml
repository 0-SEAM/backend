services:
  backend:
    container_name: seam-backend
    build:
      context: .
      dockerfile: Dockerfile
    image: seam-backend:latest
    ports:
      - "8080:8080"
    environment:
      TZ: Asia/Seoul
    restart: unless-stopped
